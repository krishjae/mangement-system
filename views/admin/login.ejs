<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="<%process.cwd()%>/https://fonts.googleapis.com/css2?family=Roboto:wght@300&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #f2f2f2;
        }

        .background-image {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            background: url('<%process.cwd()%>/images/Aerial-hp--GQ-25aug17_alamy_b.jpg') no-repeat center center fixed;
            -webkit-background-size: cover;
            -moz-background-size: cover;
            -o-background-size: cover;
            background-size: cover;
        }

        .login-container {
            background-color: rgba(255, 255, 255, 0.8);
            padding: 50px;
            border-radius: 130px;
            box-shadow: 0px 10px 20px rgba(0, 0, 0, 0.3);
            text-align: center;
            color: #333;
            animation: fadeIn 1s ease-in-out;
        }

        @keyframes fadeIn {
            0% {
                opacity: 0;
            }
            100% {
                opacity: 1;
            }
        }

        .login-container h2 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .login-form {
            margin-top: 10px;
        }

        .login-form input[type="text"],
        .login-form input[type="password"] {
            width: 80%;
            padding: 10px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 5px;
        }

        .login-form input[type="submit"] {
            background-color: #2980b9;
            color: #ffffff;
            padding: 10px 20px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        .login-form input[type="submit"]:hover {
            background-color: #1c6ea4;
        }

        .login-form p {
            font-size: 14px;
            margin-top: 10px;
        }

        .login-form a {
            color: #3498db;
        }
    </style>
</head>
<body>
    <div class="background-image"></div>
    <div class="login-container">
        <h2>Admin Login</h2>
        <form class="login-form" action="/auth/login/admin" method="post" name="login">
            <div class="form-group">
                <label for="username"><span class="login-danger"></span></label>
                <input class="form-control pass-input" type="text" id="username" name="Email" placeholder="Enter your email" required>
                <span class="profile-views feather-eye toggle-password"></span>
            </div>
            <div class="form-group">
                <label for="password"><span class="login-danger"></span></label>
                <input class="form-control pass-input" type="password" id="password" name="Password" placeholder="Enter your password" required>
                <span class="profile-views feather-eye toggle-password"></span>
            </div>
            <input type="submit" value="Login">
        </form>
    </div>
</body>
</html>